# Clerk — frontend (safe to expose, prefixed with VITE_)
VITE_CLERK_PUBLISHABLE_KEY=pk_live_...

# Stripe — server-side only (never expose secret key to the browser)
STRIPE_SECRET_KEY=sk_live_...

# Resend — server-side only
RESEND_API_KEY=re_...
